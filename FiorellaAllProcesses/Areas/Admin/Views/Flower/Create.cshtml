@model Product
@{
    ViewData["Title"] = "Create";
}

<div class="container">
    <row>
        <div class="col-md-6 grid-margin stretch-card mt-5">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title"></h4>
                    <form class="forms-sample" method="post">
                        <div class="form-group">
                            <label asp-for="Name"></label>
                            <input asp-for="Name" class="form-control" placeholder="Name">
                            <span class="text-danger" asp-validation-for="Name"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Price"></label>
                            <input asp-for="Price" class="form-control" placeholder="Price">
                            <span class="text-danger" asp-validation-for="Price"></span>
                        </div>
                        @*<div class="form-group">
                            <label asp-for="СurrencyId">Category</label>
                            <select class="form-select" aria-label="Default select example">
                                @foreach (var currency in (List<Category>)ViewBag.currency)
                                {
                                    <option value="@currency.Id" class="form-control">@currency.Name</option>
                                }
                            </select>
                            <span class="text-danger" asp-validation-for="Currency"></span>
                        </div>*@
                        @*<div class="form-group">
                            <label asp-for="CategoryId">Category</label>
                            <select asp-for="CategoryId" class="form-select" aria-label="Default select example">
                                @foreach (var category in (List<Category>)ViewBag.category)
                                {
                                    <option value="@category.Id" class="form-control">@category.Name</option>
                                }
                            </select>
                            <span class="text-danger" asp-validation-for="CategoryId"></span>
                        </div>*@
                        <button type="submit" class="btn btn-primary mr-2">Submit</button>
                        <button class="btn btn-light" asp-action="Index">Cancel</button>
                    </form>
                </div>
            </div>
        </div>
    </row>
</div>





